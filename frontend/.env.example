REACT_APP_API_URL=http://localhost:5000/api

# Works around an eslint-config-react-app / eslint-plugin-jest version
# conflict in react-scripts 5 that otherwise breaks `npm run build`.
DISABLE_ESLINT_PLUGIN=true
